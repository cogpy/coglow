name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=Release

    - name: Build
      run: cmake --build build -- -j2

    - name: Package binaries
      run: |
        mkdir release
        cp build/bin/glow-compiler release/
        tar -czvf glow-compiler-${{ github.ref_name }}-linux-amd64.tar.gz -C release .

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./glow-compiler-${{ github.ref_name }}-linux-amd64.tar.gz
        asset_name: glow-compiler-${{ github.ref_name }}-linux-amd64.tar.gz
        asset_content_type: application/gzip
